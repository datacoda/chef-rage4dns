---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: rage4dns
    run_list:
      - recipe[rage4dns::default]
      - recipe[rage4dns::dynamic]
    attributes:
      dev_mode: true
      rage4dns:
        dynamic:
          refresh_interval: 5
          records:
            foo.example.com: 5555beef
            dyn.example.com: 5555dead
